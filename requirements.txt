fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20
pydantic>=2.0

# Core numeric / image stack (numpy 2.x compatible)
numpy>=2.0,<2.3
Pillow>=10.0

# MediaPipe 0.10.30+ supports numpy>=2 (no longer pins numpy<2)
# It depends on opencv-contrib-python; we override with the headless variant
# after install so the Docker image stays lightweight and headless-friendly.
mediapipe>=0.10.30,<1.1

torch==2.8.0
torchvision==0.23.0

transformers==4.55.2

yt-dlp>=2025.8.22

requests>=2.32

scikit-image>=0.24
scipy>=1.14
